using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CircularPattern : BulletPattern
{
    /*public override IEnumerator FirePattern(Transform spawnerTransform)
    {
        while (true) // Continuous firing; can be adjusted based on need
        {
            for (int i = 0; i < bulletsPerGroup; i++)
            {
                // Instantiate bullet and set its speed and direction
                // Placeholder for bullet instantiation logic
                yield return new WaitForSeconds(intervalBetweenBullets);
            }
            yield return new WaitForSeconds(intervalBetweenGroups);
        }
    }*/
}
